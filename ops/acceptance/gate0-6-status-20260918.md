# Gate 0–6 completion status — 2026-09-18

## Decision

**NO-GO. Do not push `main`, deploy, archive, or upload TestFlight from this candidate.**
Build 41 remains non-final. No Build 42 claim is made by this worktree.

## Gate status

| Gate | Status | Evidence |
|---|---|---|
| Gate 0 — preserve WIP / clean completion worktree | PASS | WIP preservation commits `2f606b4faf7fd1a41d7826c2c1ea0b5404e05891` and `d239c2fc2675fd0d621943040d2bf7605db3641f`; snapshots `/Users/dengzhaoyu/Projects/ai-lab-gate0-6-evidence-20260917/wip-snapshot.tsv` and `wip-snapshot-after-concurrent-work.json`; completion worktree based on `10ddeaf6bc2a5036dcedbdd195397cb4a5067860` |
| Gate 1 — Gateway security | PARTIAL | Targeted Gateway/product tests pass; bypass scan reports 0; full backend passes. Capability scope remains incomplete, so all-write-path authority is not proven for absent capabilities. |
| Gate 2 — capability batches | FAIL | PCM iOS scope v2 has 70 requested capabilities; generated matrix reports 24 absent, 46 partial, 0 implemented. Schedule list/create/update/delete now have locally executable contracts; notification capabilities remain absent and production receipts remain unverified. |
| Gate 3 — backend/iOS automated acceptance | PARTIAL | Python full-suite baseline, current focused Gateway/QCP/QWS suites, and iOS unit baseline pass. Full capability handler and simulator matrix cannot pass while 24 capabilities are absent. |
| Gate 4 — production clean-room | FAIL | No complete per-capability production receipts/readbacks/screenshots exist. |
| Gate 5 — same-SHA commit/deploy verification | NOT STARTED | Correctly blocked by failed capability and production gates. |
| Gate 6 — final TestFlight | NOT STARTED | Correctly blocked. Build 41 is not reused and this worktree does not assign Build 42. |

## Verified results

### Backend

- Command environment:
  - `RESEARCH_PIPELINE_MODULE=/Users/dengzhaoyu/Projects/ai-lab-vault-governance/tools/article_research_pipeline.py`
  - `HERMES_SOURCE=/Users/dengzhaoyu/.hermes/hermes-agent`
  - Hermes source precedes repository source in `PYTHONPATH` to avoid the repository's unrelated top-level `tools` package shadowing Hermes `tools.registry`.
- Result: **2637 passed, 17 skipped, 0 failed, 16 subtests passed**; exit `0`.
- Log: `/Users/dengzhaoyu/Projects/ai-lab-gate0-6-evidence-20260917/pytest-gate-corrected.log`
- JUnit: `/Users/dengzhaoyu/Projects/ai-lab-gate0-6-evidence-20260917/pytest-gate-corrected.xml`
- Exit receipt: `/Users/dengzhaoyu/Projects/ai-lab-gate0-6-evidence-20260917/pytest-gate-corrected.exit`
- Skip classification: 2 superseded V1 fixtures, 14 installed-catalog opt-ins, 1 private local Vault opt-in.

### iOS unit tests

- Device: iPhone 17 Pro, iOS 26.1, `A5005DE7-3D7E-4FA0-A9D9-92967B4A699A`
- Result: **213 passed, 0 failed, 0 skipped**; exit `0`.
- Result bundle: `/tmp/QuantumnGateCurrentUnit.xcresult`
- Log: `/Users/dengzhaoyu/Projects/ai-lab-gate0-6-evidence-20260917/ios-unit-current.log`
- Exit receipt: `/Users/dengzhaoyu/Projects/ai-lab-gate0-6-evidence-20260917/ios-unit-current.exit`

### Gateway and PCM checks

- Gateway bypass scan: `0` violations.
- Matrix generation/check: deterministic and current.
- Matrix close check: fails closed with 70 incomplete capabilities.
- Current matrix: `implemented=0`, `partial=46`, `absent=24`, `unverified=0`.
- Existing simulator UI inventory: 16 test methods across 2 files; it does not cover the required complete capability/scenario product.

## Changes in this candidate

- Hardened proposal confirmation/session binding, required idempotency, CAS resource version binding, versioned durable receipts, restart reconciliation, and status readback on the existing Gateway path.
- Removed the service-layer dependency on `backend.api.workflows`; workflow CAS lookup is owner/tenant scoped in the service layer.
- Fixed positional-to-keyword Hermes worker binding so client session identity cannot shift into another authority-bearing parameter.
- Added deterministic PCM iOS scope/matrix generation and a fail-closed `--require-complete` gate.
- Added a static Gateway bypass scanner and tests.
- Fixed direct execution of `scripts/publication_operator.py` so repository imports resolve without relying on the caller's current directory.
- Updated stale worker test doubles to the keyword-only authority boundary.
- Added `project.list/create/open` and `task.list/create/update/status` to the existing Gateway/QWS chain with versioned contracts, bindings, owner/member policy, events, iOS fallback routing, confirmation/idempotency on writes, and revision-CAS domain reuse.
- Kept `task.execute` absent: the only real QWS auto-execution API requires an authenticated Taskboard bearer context and task-conversation lifecycle prerequisites that the durable Gateway replay payload does not retain safely.
- Added `workflow.approve` and `workflow.revise` through the existing Workflow handlers. Approval now accepts an optional domain-bound plan hash/revision CAS token; QCP requires it. Revision reuses the immutable plan editor's CAS and request-id replay.
- Kept `workflow.cancel` absent: the execution cancellation handler has no domain idempotency key or durable cancellation receipt, so a process crash after cancellation but before QCP receipt persistence cannot safely distinguish replay from an unrelated prior cancellation.
- Added `schedule.list/create/update/delete` over the canonical QWS project process and project-change proposal owner. Writes require Gateway confirmation/idempotency plus QWS process-revision CAS and durable request replay/conflict handling; no schedule store or runtime was added.
- Kept all three notification capabilities absent: notifications are tenant-global with no user owner, per-user read state, CAS/idempotency receipt, or preferences persistence/API.

## Release blockers

1. Add and verify schemas, bindings, policies, handlers, events, renderers, tests, iOS/QWS consumers, and production receipts for the 24 absent capabilities; notification capabilities need per-user domain ownership/persistence, `task.execute` needs a safe server-side delegated-auth/domain entry, and `workflow.cancel` needs a durable domain cancellation receipt/idempotency contract before registration.
2. Close production evidence for the 46 partial capabilities, including Schedule, Project/Task, Word, research report, academic paper, PPTX, knowledge notes, and workflow paths.
3. Build a contract-driven simulator matrix for every capability and required failure/recovery scenario; then perform real simulator clicks.
4. Run production clean-room acceptance with a dedicated tenant and retain IDs, events, receipts, revisions, hashes, byte readbacks, and screenshots/xcresults.
5. Only after all preceding gates pass: create final commit, push `main`, deploy the same SHA, verify production revision, query ASC, select the next unused build number, archive from a fresh clone of that SHA, upload, and read back TestFlight processing/group/installability.
